import React from 'react';
import styles from './Footer.module.scss'; 

const Body = () => {
    return (
        <div className={styles.footer_container}> 
        </div>
    );
};

export default Body;
